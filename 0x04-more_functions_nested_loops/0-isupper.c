#include "main.h"

/**
 * _isupper - Code to check uper case number
 *@c: variable or character to be checked
 * Return: 1 if char is upper ; 0 if char is lower
*/
int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')
	
		return (1);
	
	return (0);
	
}
