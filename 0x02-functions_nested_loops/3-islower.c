#include "main.h"

/**
 * _islower - check numbers casing
 *
 * @c: var name
 * Return: Always 0 success
*/
int _islower(int c)
{
	/* declare local variable */

	if (_islower(c))
	{
		_putchar(1);
	}

	else
	{
		_putchar(0);
	}

	putchar('\n');

	return (0);
}
