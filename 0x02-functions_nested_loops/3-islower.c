#include "main.h"

/**
 * _islower - check numbers casing
 *
 * @c: var name
 * Return: Always 0 success
*/
int _islower(int c)
{
	/* declare local variable */

	if (_islower(c = 'H')
	{
		putchar(1);
	}
	else
	{
		putchar(0);
	}

	if (_islower(c = 'o')
	{
		putchar(1);
	}
	else
	{
		putchar(0);
	}

	if (_islower(c = '108')
	{
		putchar(1);
	}
	else
	{
		putchar(0);
	}

	putchar('\n');

	return (0);
}
