#include "main.h"

/**
 * _abs - print the absolute value of an integer
 * @n: var
 * Return: Absolute value
*/
int _abs(int n)
{
	if (n > 0)
	{
		return (n);
	}

	else if (n < 0)
	{
		return (-n);
	}
		return (0);
}
